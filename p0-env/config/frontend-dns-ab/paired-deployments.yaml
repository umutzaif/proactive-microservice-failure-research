apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-dns-ab-control
  namespace: online-boutique
  labels:
    app: frontend-dns-ab-control
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-dns-ab-control
  template:
    metadata:
      labels:
        app: frontend-dns-ab-control
    spec:
      serviceAccountName: frontend
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: server
          image: us-central1-docker.pkg.dev/online-boutique-ci/microservices-demo/frontend:v0.10.6
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          readinessProbe:
            initialDelaySeconds: 10
            httpGet:
              path: /_healthz
              port: 8080
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          env:
            - name: PORT
              value: "8080"
            - name: PRODUCT_CATALOG_SERVICE_ADDR
              value: productcatalogservice:3550
            - name: CURRENCY_SERVICE_ADDR
              value: currencyservice:7000
            - name: CART_SERVICE_ADDR
              value: cartservice:7070
            - name: RECOMMENDATION_SERVICE_ADDR
              value: recommendationservice:8080
            - name: SHIPPING_SERVICE_ADDR
              value: shippingservice:50051
            - name: CHECKOUT_SERVICE_ADDR
              value: checkoutservice:5050
            - name: AD_SERVICE_ADDR
              value: adservice:9555
            - name: SHOPPING_ASSISTANT_SERVICE_ADDR
              value: shoppingassistantservice:80
            - name: ENABLE_PROFILER
              value: "0"
            - name: DISABLE_PROFILER
              value: "1"
            - name: ENV_PLATFORM
              value: local
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-dns-ab-control
  namespace: online-boutique
spec:
  selector:
    app: frontend-dns-ab-control
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-dns-ab-treatment
  namespace: online-boutique
  labels:
    app: frontend-dns-ab-treatment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend-dns-ab-treatment
  template:
    metadata:
      labels:
        app: frontend-dns-ab-treatment
    spec:
      serviceAccountName: frontend
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
        - name: server
          image: makale/frontend:v0.10.6-env-platform-v1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          readinessProbe:
            initialDelaySeconds: 10
            httpGet:
              path: /_healthz
              port: 8080
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            readOnlyRootFilesystem: true
          env:
            - name: PORT
              value: "8080"
            - name: PRODUCT_CATALOG_SERVICE_ADDR
              value: productcatalogservice:3550
            - name: CURRENCY_SERVICE_ADDR
              value: currencyservice:7000
            - name: CART_SERVICE_ADDR
              value: cartservice:7070
            - name: RECOMMENDATION_SERVICE_ADDR
              value: recommendationservice:8080
            - name: SHIPPING_SERVICE_ADDR
              value: shippingservice:50051
            - name: CHECKOUT_SERVICE_ADDR
              value: checkoutservice:5050
            - name: AD_SERVICE_ADDR
              value: adservice:9555
            - name: SHOPPING_ASSISTANT_SERVICE_ADDR
              value: shoppingassistantservice:80
            - name: ENABLE_PROFILER
              value: "0"
            - name: DISABLE_PROFILER
              value: "1"
            - name: ENV_PLATFORM
              value: local
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-dns-ab-treatment
  namespace: online-boutique
spec:
  selector:
    app: frontend-dns-ab-treatment
  ports:
    - port: 80
      targetPort: 8080
