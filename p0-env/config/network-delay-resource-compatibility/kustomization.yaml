apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../network-delay-design
patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      name: recommendationservice
    path: recommendation-server-cpu-limit.json
