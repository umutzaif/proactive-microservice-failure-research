# ob-network-resource-compat-004 ön-kaydı

Bu benzersiz replacement yalnız D-053 kapsamında `KJson` parametresini PowerShell'in
otomatik `$Args` değişkeninden ayırır. D-050'nin 500m server CPU limiti, 100m request,
memory, probe, proxy, image, `ob-second-15u-1r-v1` workload'u ve bütün prospektif
geçerlilik eşikleri değişmez.

Run ancak bu commit canonical merge edildikten ve kullanıcı ayrı canlı yürütme onayı
verdikten sonra başlatılabilir. `001`, `002` ve `003` invalid kalır; hiçbir run ID
yeniden kullanılmaz. Toxic/fault, model, LLM ve GAT yasaktır.
