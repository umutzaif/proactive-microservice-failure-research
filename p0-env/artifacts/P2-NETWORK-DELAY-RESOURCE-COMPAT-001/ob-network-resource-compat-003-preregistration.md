# ob-network-resource-compat-003 ön-kaydı

Bu benzersiz replacement, yalnız D-052 native stdout/stderr kanal izolasyonunu ekler.
D-050'nin 500m server CPU limiti, 100m request, memory, probe, proxy, image,
`ob-second-15u-1r-v1` workload'u ve bütün prospektif geçerlilik eşikleri değişmez.

Run ancak bu commit canonical merge edildikten ve kullanıcı ayrı canlı yürütme onayı
verdikten sonra başlatılabilir. `ob-network-resource-compat-001` ve `002` invalid
kalır; hiçbir ID yeniden kullanılmaz. Toxic/fault, model, LLM ve GAT yasaktır.
